PROJECT(imumodel)
cmake_minimum_required(VERSION 2.6)

include_directories(tasks)
SET (CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/.orogen/config")
INCLUDE(imumodelBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

